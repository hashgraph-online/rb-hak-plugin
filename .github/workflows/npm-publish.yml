name: npm publish (legacy)

on:
  workflow_dispatch: {}

jobs:
  redirect:
    runs-on: ubuntu-latest
    steps:
      - name: Use trusted publish workflow
        run: echo "Use .github/workflows/publish.yml for npm trusted publishing."
